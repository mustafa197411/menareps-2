import type { User } from "firebase/auth";
export async function transitionCommercialOrder(user: Pick<User, "getIdToken">, request: { orderId: string; action: string; comments?: string; expectedVersion: string; metadata?: Record<string, unknown> }, fetchImplementation: typeof fetch = fetch) { const token = await user.getIdToken(); const response = await fetchImplementation("/api/orders/commercial/transition", { method: "POST", headers: { Authorization: `Bearer ${token}`, "Content-Type": "application/json" }, body: JSON.stringify(request) }); const payload = await response.json(); if (!response.ok || !payload.success) { const error = new Error(payload.code || "ORDER_TRANSITION_FAILED") as Error & { code?: string }; error.code = payload.code; throw error; } return payload; }
